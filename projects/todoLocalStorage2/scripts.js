let addTaskBtn = document.getElementsByClassName('add-task-btn')[0]
let taskList = []

addTaskBtn.addEventListener('click', () => {
    let task = document.getElementsByClassName('task-input')[0].
    console.log(task);
    
})